# 🦋 M0THB0T

A Telegram bot created by @MOTHHHHHHHHHH

## Status
🚧 Currently in development

## Setup
1. Clone this repository
2. Install dependencies: `pip install -r requirements.txt`
3. Set your bot token:
   - Create a `.env` file (NEVER commit this!)
   - OR set environment variable: `export BOT_TOKEN="your_token_here"`
4. Run: `python bot.py`

## Commands
- `/start` - Start the bot
- `/whatis` - Bot description
- `/help` - Show all commands

## Deployment
This bot is designed to run 24/7 on Render.com, Railway, or similar services.

## Security
⚠️ Never commit your bot token to GitHub!
Always use environment variables.
